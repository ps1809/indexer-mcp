package com.projectiq.mcp.client.dto;

/**
 * Enum representing the types of Java classes supported by the find_class tool.
 */
public enum ClassType {
    CLASS,
    INTERFACE,
    ENUM,
    RECORD,
    ANNOTATION
}